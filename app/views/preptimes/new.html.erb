<%= render 'shared/page_title', title: 'Create a New Preptime'%>

<%= render 'shared/errors', obj: @preptime %>
<div class="row">
    <div class="well col-md-8 col-md-offset-2">
        <%= form_for @preptime do |f| %>
            <%= f.label :name %>
            <%= f.text_field :time %>

             <%= f.submit 'Create Preptime', class: "btn btn-success" %>
        <% end %>
    </div>
</div>