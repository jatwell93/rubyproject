<div class="container">
  <div class="row search-result">
    <% @athletes.each do |athlete| %>
    <div class= "col-md-10 col-xs-offset-1 well member" > 
      <%=athlete.full_name%>
    </div>
    <%end%>
  </div>
  <%= will_paginate @athletes, renderer: BootstrapPagination::Rails %>
</div>