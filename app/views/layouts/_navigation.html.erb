
<nav class="navbar navbar-inverse">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <div id="logo">
        <%= link_to root_path, class: "btn btn-default btn-success", id: "logo" do %>
                                myRecipes
          <i class="fa fa-cutlery" aria-hidden="true"></i>
        <% end %>
       
      </div>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <!--<ul class="nav navbar-nav">-->
      <ul class="nav_ul_item">
        <li class="dropdown"><%= link_to '#', class: "btn btn-default btn-danger dropdown-toggle", "data-toggle" => "dropdown" do %>
            Recipes <b class="caret"></b>
              <% end %>
              <ul class="dropdown-menu">
                <li class="dropdown-header">Search by</li>
                 <li class="dropdown-submenu">
                  <a href="#" class="dropdown-toggle" data-toggle="dropdown">Styles</a>
                    <ul class="dropdown-menu">
                      <% Style.all.each do |style| %>
                        <li id="styles"><%= link_to style.name, style_path(style) %></li>
                      <% end %>
                <% if logged_in? %>
                <li class="divider"></li>
                  <li>
                    <%= link_to(new_style_path) do %>
                      New Style
                    <% end %>
                  </li>
                <% end %>
                </li>
                  </ul>
                    <li class="divider"></li>
                      <li class="dropdown-submenu">
                        <a href="#" class="dropdown-toggle" data-toggle="dropdown">Ingredients</a>
                          <ul class="dropdown-menu">
                            <% Ingredient.all.each do |ingredient| %>
                              <li id="ingredients"><%= link_to ingredient.name, ingredient_path(ingredient) %></li>
                            <% end %>
                <% if logged_in? %>
                  <li class="divider"></li>
                    <li>
                      <%= link_to(new_ingredient_path) do %>
                        New Ingredient
                      <% end %>
                  </li>
                <% end %>
                  </li>
                    </ul>
                      <li class="divider"></li>
                        <li class="dropdown-submenu">
                          <a href="#" class="dropdown-toggle" data-toggle="dropdown">Calories</a>
                            <ul class="dropdown-menu">
                              <% Calorie.all.each do |calorie| %>
                                <li id="calories"><%= link_to calorie.amount, calory_path(calorie) %></li>
                              <% end %>
                <% if logged_in? %>
                  <li class="divider"></li>
                    <li>
                      <%= link_to(new_calory_path) do %>
                        New Calorie
                      <% end %>
                    </li>
                <% end %>
                  </li>
                    </ul>
                      <li class="divider"></li>
                        <li class="dropdown-submenu">
                          <a href="#" class="dropdown-toggle" data-toggle="dropdown">Feeds</a>
                            <ul class="dropdown-menu">
                              <% Feed.all.each do |feed| %>
                                <li id="feeds"><%= link_to feed.people, feed_path(feed) %></li>
                              <% end %>
                <% if logged_in? %>
                  <li class="divider"></li>
                    <li>
                      <%= link_to(new_feed_path) do %>
                        New Feeds
                      <% end %>
                  </li>
                <% end %>
                  </li>
                    </ul>
                      <li class="divider"></li>
                        <li class="dropdown-submenu">
                          <a href="#" class="dropdown-toggle" data-toggle="dropdown">Preptime</a>
                            <ul class="dropdown-menu">
                              <% Preptime.all.each do |preptime| %>
                                <li id="preptimes"><%= link_to preptime.time, preptime_path(preptime) %></li>
                              <% end %>
                <% if logged_in? %>
                  <li class="divider"></li>
                    <li>
                      <%= link_to(new_preptime_path) do %>
                        New preptime
                      <% end %>
                    </li>
                <% end %>
                  </li>
                    </ul>
                      <% if logged_in? %>
                        <li class="divider"></li>
                          <li class="dropdown-header">Create</li>
                            <li>
                              <%= link_to(new_recipe_path) do %>
                                New Recipe
                              <% end %>
                            </li>
                      <% end %>
            </ul>
        </ul> 
        <!--ends style button-->
        <% if logged_in? %>
          <ul class="nav_ul_item">
            <li class="dropdown"><%= link_to '#', class: "btn btn-default btn-danger dropdown-toggle", "data-toggle" => "dropdown" do %>
              <%= current_user.chefname %> <b class="caret"></b>
              <% end %>
              <ul class="dropdown-menu">
                <li><%= link_to "View Profile", chef_path(current_user) %> </li>
                <li><%= link_to "Edit Profile", edit_chef_path(current_user) %> </li>
                <li class="divider"></li>
                <li><%= link_to "Logout", logout_path %></li>
              </ul>
            </li>
          </ul>
        <% else %>
        <div class="nav_item">
          <%= link_to login_path, class: "btn btn-default btn-primary" do %>
            <i class="fa fa-sign-in" aria-hidden="true"></i>
                                Login
          <% end %>
        </div>
        <div class="nav_item">
          <%= link_to register_path, class: "btn btn-default btn-success" do %>
            <i class="fa fa-user-plus" aria-hidden="true"></i>
                                Signup
            <% end %>
          <% end %>
        </div>
        


    </div><!-- /.navbar-collase -->
    <div class="search">
  <%= form_tag search_recipes_path, :method => 'get', class: 'input-group'  do %>
    <%= text_field_tag :search, params[:search], class: 'form-control', placeholder: 'Search for Recipes' %>
    <span class="input-group-btn">
      <%= button_tag(type: "submit", class: "btn btn-default btn-success") do %>
      Search <i class="fa fa-search" aria-hidden="true"></i>
      <% end %>
    </span>
  <% end %>
</div>        
  </div><!-- /.contaner-fluid -->
</nav>